// examples/demo.ltl — runnable demo for ltl_asn1

import std::io
use ltl_asn1 as lib

fn main() {
    io::println("ltl_asn1 demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}